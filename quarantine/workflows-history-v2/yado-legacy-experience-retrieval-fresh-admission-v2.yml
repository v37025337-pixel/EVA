name: YADO Legacy Experience Retrieval Fresh Admission V2
on:
  push:
    branches: [yado-architecture-shadow-search]
    paths:
      - "architecture/yado-legacy-experience-fresh-admission-v2-request.json"
permissions:
  contents: write
concurrency:
  group: yado-legacy-experience-fresh-admission-v2
  cancel-in-progress: true
jobs:
  admit:
    runs-on: ubuntu-latest
    timeout-minutes: 18
    steps:
      - uses: actions/checkout@v4
        with: { fetch-depth: 1 }
      - uses: actions/setup-python@v5
        with: { python-version: '3.12' }
      - name: Run V2 fresh admission
        id: admit
        continue-on-error: true
        run: python runtime/yado_legacy_experience_retrieval_fresh_admission_v2.py
      - name: Upload evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: yado-legacy-experience-fresh-admission-v2
          path: |
            runtime/yado_legacy_experience_retrieval_fresh_admission_v2_receipt.json
            architecture/evolution-ledger.json
          if-no-files-found: warn
          retention-days: 30
      - name: Persist evidence
        if: always()
        run: |
          mkdir -p receipts
          if [ -f runtime/yado_legacy_experience_retrieval_fresh_admission_v2_receipt.json ]; then
            cp runtime/yado_legacy_experience_retrieval_fresh_admission_v2_receipt.json "receipts/yado-legacy-experience-retrieval-fresh-admission-v2-run-${GITHUB_RUN_ID}.json"
          fi
          git config user.name "YADO Legacy Experience Admission V2"
          git config user.email "actions@users.noreply.github.com"
          files=(architecture/evolution-ledger.json)
          if [ -f "receipts/yado-legacy-experience-retrieval-fresh-admission-v2-run-${GITHUB_RUN_ID}.json" ]; then files+=("receipts/yado-legacy-experience-retrieval-fresh-admission-v2-run-${GITHUB_RUN_ID}.json"); fi
          git add "${files[@]}"
          if git diff --cached --quiet; then echo unchanged; else
            git commit -m "Persist V2 fresh admission for self-evolved legacy retriever [skip ci]"
            git push origin HEAD:yado-architecture-shadow-search
          fi
      - name: Enforce verdict
        if: always()
        run: |
          python - <<'PY'
          import json,pathlib
          p=pathlib.Path("runtime/yado_legacy_experience_retrieval_fresh_admission_v2_receipt.json")
          if not p.exists(): raise SystemExit("MISSING_V2_ADMISSION_RECEIPT")
          r=json.loads(p.read_text())
          if r.get("status")!="PASS_LEGACY_EXPERIENCE_RETRIEVAL_FRESH_ADMISSION_V2":
              raise SystemExit("SEMANTIC_WITHHOLD:"+str(r.get("status")))
          print(r["status"])
          PY
