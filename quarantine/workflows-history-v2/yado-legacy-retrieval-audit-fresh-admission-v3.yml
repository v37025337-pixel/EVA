name: YADO Legacy Retrieval Audit Fresh Admission V3
on:
  push:
    branches: [yado-architecture-shadow-search]
    paths:
      - "architecture/yado-legacy-retrieval-audit-fresh-admission-v3-request.json"
permissions:
  contents: write
concurrency:
  group: yado-legacy-retrieval-audit-fresh-admission-v3
  cancel-in-progress: true
jobs:
  admit:
    runs-on: ubuntu-latest
    timeout-minutes: 18
    steps:
      - uses: actions/checkout@v4
        with: { fetch-depth: 1 }
      - uses: actions/setup-python@v5
        with: { python-version: '3.12' }
      - run: python runtime/reconstruct_rc8v36.py
      - run: python -m pip install --disable-pip-version-check -r runtime/yado_rc8_v36/requirements-github.txt
      - name: Run fresh admission V3
        id: admit
        continue-on-error: true
        run: python runtime/yado_legacy_retrieval_audit_fresh_admission_v3.py
      - name: Upload evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: yado-legacy-retrieval-audit-fresh-admission-v3
          path: |
            runtime/yado_legacy_retrieval_audit_fresh_admission_v3_receipt.json
            architecture/evolution-ledger.json
          if-no-files-found: warn
          retention-days: 30
      - name: Persist evidence
        if: always()
        run: |
          mkdir -p receipts
          if [ -f runtime/yado_legacy_retrieval_audit_fresh_admission_v3_receipt.json ]; then
            cp runtime/yado_legacy_retrieval_audit_fresh_admission_v3_receipt.json "receipts/yado-legacy-retrieval-audit-fresh-admission-v3-run-${GITHUB_RUN_ID}.json"
          fi
          git config user.name "YADO Legacy Retrieval Audit Admission"
          git config user.email "actions@users.noreply.github.com"
          files=(architecture/evolution-ledger.json)
          if [ -f "receipts/yado-legacy-retrieval-audit-fresh-admission-v3-run-${GITHUB_RUN_ID}.json" ]; then files+=("receipts/yado-legacy-retrieval-audit-fresh-admission-v3-run-${GITHUB_RUN_ID}.json"); fi
          git add "${files[@]}"
          if git diff --cached --quiet; then echo unchanged; else
            git commit -m "Persist YADO legacy retrieval audit fresh admission V3 [skip ci]"
            git push origin HEAD:yado-architecture-shadow-search
          fi
      - name: Enforce verdict
        if: always()
        run: |
          python - <<'PY'
          import json,pathlib
          p=pathlib.Path("runtime/yado_legacy_retrieval_audit_fresh_admission_v3_receipt.json")
          if not p.exists(): raise SystemExit("MISSING_V3_ADMISSION_RECEIPT")
          r=json.loads(p.read_text())
          if r.get("status")!="PASS_LEGACY_RETRIEVAL_AUDIT_FRESH_ADMISSION_V3":
              raise SystemExit("SEMANTIC_WITHHOLD:"+str(r.get("status")))
          print(r["status"])
          PY
