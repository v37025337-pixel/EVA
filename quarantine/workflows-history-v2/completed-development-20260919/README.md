# Completed development campaign workflows

These four read-only, branch-specific experiment workflows have completed their
runs. Their exact bytes are preserved here as non-executable history; the Python
experiment entry points and recorded results remain available in the repository.
The active single-core workflow inventory and its 131-workflow limit are unchanged.

The compositional upgrade ran successfully at commit
`984c2fc3026139cf2f174417b66e04f784baef00`, Actions run `35447286863`.
Reproduction uses the commands in the preserved workflow; this archive does not
register another active scheduler or grant repository write access.
