name: YADO G2 Evolutionary Successor Genesis V3
on:
  push:
    branches: [yado-architecture-shadow-search]
    paths:
      - "architecture/yado-kernel-evolutionary-successor-genesis-v3-request.json"
permissions:
  contents: write
jobs:
  evolve:
    runs-on: ubuntu-latest
    timeout-minutes: 8
    steps:
      - uses: actions/checkout@v4
        with: {fetch-depth: 0}
      - uses: actions/setup-python@v5
        with: {python-version: '3.12'}
      - run: python runtime/reconstruct_rc8v36.py
      - run: python -m pip install --disable-pip-version-check -r runtime/yado_rc8_v36/requirements-github.txt
      - name: Create G2 evolutionary RuleProgram successor V3
        run: python runtime/yado_kernel_evolutionary_successor_genesis_v3.py
      - name: Persist successor V3 evidence and frontier
        run: |
          mkdir -p receipts candidates/kernel-self-generated
          cp runtime/yado_kernel_evolutionary_successor_genesis_v3_receipt.json "receipts/yado-kernel-evolutionary-successor-genesis-v3-run-${GITHUB_RUN_ID}.json"
          git config user.name "YADO G2 Evolutionary Successor V3"
          git config user.email "actions@users.noreply.github.com"
          git add architecture/evolution-ledger.json architecture/yado-kernel-evolutionary-successor-genesis-v3.json canonical/yado-main-head-g2.json canonical/yado-unified-core-v1.json candidates/kernel-self-generated/evolutionary-rule-program-successor-v3.json "receipts/yado-kernel-evolutionary-successor-genesis-v3-run-${GITHUB_RUN_ID}.json"
          git commit -m "Persist G2 evolutionary RuleProgram successor V3 [skip ci]"
          git pull --rebase origin yado-architecture-shadow-search
          git push origin HEAD:yado-architecture-shadow-search
      - name: Enforce V3 receipt
        run: |
          python - <<'PY'
          import json
          r=json.load(open("runtime/yado_kernel_evolutionary_successor_genesis_v3_receipt.json"))
          assert r["status"]=="PASS_EVOLUTIONARY_SUCCESSOR_GENESIS_V3",r
          assert all(r["checks"].values()),r
          print(r["candidate_state"],r["metrics"],r["next_required_capability"])
          PY
