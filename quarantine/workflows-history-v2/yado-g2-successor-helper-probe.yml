name: YADO G2 Successor Helper Probe
on:
  push:
    branches: [yado-architecture-shadow-search]
    paths:
      - "architecture/yado-g2-successor-helper-probe-request.json"
permissions:
  contents: write
jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: {python-version: '3.12'}
      - run: python runtime/reconstruct_rc8v36.py
      - run: python -m pip install --disable-pip-version-check -r runtime/yado_rc8_v36/requirements-github.txt
      - run: python runtime/yado_g2_successor_helper_probe.py
      - run: |
          mkdir -p receipts
          cp runtime/yado_g2_successor_helper_probe_receipt.json "receipts/yado-g2-successor-helper-probe-run-${GITHUB_RUN_ID}.json"
          git config user.name "YADO G2 Successor Helper Probe"
          git config user.email "actions@users.noreply.github.com"
          git add "receipts/yado-g2-successor-helper-probe-run-${GITHUB_RUN_ID}.json"
          git commit -m "Persist successor helper probe [skip ci]"
          git pull --rebase origin yado-architecture-shadow-search
          git push origin HEAD:yado-architecture-shadow-search
