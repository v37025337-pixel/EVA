name: YADO Composed Sequence Transform V1
on:
  push:
    branches: [yado-architecture-shadow-search]
    paths:
      - "runtime/yado_composed_sequence_transform_v1.py"
      - ".github/workflows/yado-composed-sequence-transform-v1.yml"
permissions:
  contents: write
jobs:
  compose:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: { python-version: '3.12' }
      - run: python runtime/reconstruct_rc8v36.py
      - run: python -m pip install --disable-pip-version-check -r runtime/yado_rc8_v36/requirements-github.txt
      - name: Run native filter-order composition
        run: python runtime/yado_composed_sequence_transform_v1.py
      - name: Upload receipt and component
        uses: actions/upload-artifact@v4
        with:
          name: yado-composed-sequence-transform-v1
          path: |
            runtime/yado_composed_sequence_transform_v1_receipt.json
            runtime/composed_sequence_transform_v1/component.json
          retention-days: 30
      - name: Persist evidence
        run: |
          mkdir -p receipts candidates/composed-sequence-transform-v1
          cp runtime/yado_composed_sequence_transform_v1_receipt.json receipts/yado-composed-sequence-transform-v1-latest.json
          cp runtime/composed_sequence_transform_v1/component.json candidates/composed-sequence-transform-v1/component.json
          git config user.name "YADO Composition Runner"
          git config user.email "actions@users.noreply.github.com"
          git add receipts/yado-composed-sequence-transform-v1-latest.json candidates/composed-sequence-transform-v1/component.json
          if git diff --cached --quiet; then
            echo unchanged
          else
            git commit -m "Persist composed sequence transform evidence [skip ci]"
            git push origin HEAD:yado-architecture-shadow-search
          fi
