# Legacy workflow quarantine

These workflows are historical control-plane artifacts. They are deliberately outside the GitHub workflow execution directory, so they cannot auto-execute. Their original commits remain part of YADO history and exact evidence is retrieved by registered commit SHA.
