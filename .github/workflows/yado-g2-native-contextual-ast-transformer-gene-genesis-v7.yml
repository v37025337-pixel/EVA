name: YADO G2 Native Contextual AST Transformer Gene Genesis V7
on:
  push:
    branches: [yado-architecture-shadow-search]
    paths:
      - "architecture/yado-kernel-native-contextual-ast-transformer-gene-genesis-v7-request.json"
permissions:
  contents: write
concurrency:
  group: yado-g2-native-contextual-ast-transformer-gene-genesis-v7
  cancel-in-progress: true
jobs:
  genesis:
    runs-on: ubuntu-latest
    timeout-minutes: 40
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
        with: {fetch-depth: 0}
      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065
        with: {python-version: '3.12'}
      - name: Reconstruct inherited runtime
        run: python runtime/reconstruct_rc8v36.py
      - name: Install dependencies
        run: python -m pip install --disable-pip-version-check -r runtime/yado_rc8_v36/requirements-github.txt
      - name: Canonical guard before V7
        run: python runtime/yado_canonical_invariant_guard_v1.py
      - name: Compile V7
        run: python -m py_compile runtime/yado_g2_native_contextual_ast_transformer_gene_genesis_v7.py
      - name: Run native contextual AST transformer gene genesis V7
        id: genesis
        continue-on-error: true
        run: python runtime/yado_g2_native_contextual_ast_transformer_gene_genesis_v7.py
      - name: Canonical guard after V7
        if: always()
        run: python runtime/yado_canonical_invariant_guard_v1.py
      - name: Persist V7 evidence
        if: always()
        run: |
          git config user.name "YADO Contextual AST Transformer"
          git config user.email "actions@users.noreply.github.com"
          if [ -f candidates/kernel-self-generated/g2-native-contextual-ast-transformer-gene-genesis-v7.json ]; then
            git add candidates/kernel-self-generated/g2-native-contextual-ast-transformer-gene-genesis-v7.json
          fi
          if [ -f candidates/kernel-self-generated/g2-native-contextual-ast-transformer-gene-v7.json ]; then
            git add candidates/kernel-self-generated/g2-native-contextual-ast-transformer-gene-v7.json
          fi
          git diff --cached --quiet || git commit -m "Persist YADO contextual AST transformer gene V7 [skip ci]"
          git reset --hard HEAD
          git clean -fd runtime/__pycache__ runtime/yado_rc8_v36/__pycache__ || true
          git pull --rebase origin yado-architecture-shadow-search
          git push origin HEAD:yado-architecture-shadow-search
      - name: Enforce strict V7 verdict
        if: always()
        run: |
          python - <<'PY'
          import json,pathlib
          p=pathlib.Path("candidates/kernel-self-generated/g2-native-contextual-ast-transformer-gene-genesis-v7.json")
          if not p.exists(): raise SystemExit("MISSING_CONTEXTUAL_AST_GENE_V7")
          r=json.loads(p.read_text());c=r["checks"]
          for k in ("host_authored_transition_action","host_authored_ast_subtree",
                    "host_authored_transform_condition","external_models_used",
                    "automatic_canonical_promotion"):
              assert c[k] is False,(k,r)
          assert c["canonical_unchanged"] is True,r
          if r["status"]!="PASS_SHADOW_G2_NATIVE_CONTEXTUAL_AST_TRANSFORMER_GENE_GENESIS_V7":
              raise SystemExit("SEMANTIC_WITHHOLD:"+str(r["status"]))
          assert r["gene"] and r["gene"]["actual_ast_materialization_proven"] is False,r
          assert r["gene"]["python_source_emission_proven"] is False,r
          print(r["status"],r["gene"]["gene_id"],r["receipt_sha256"])
          PY
      - uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        if: always()
        with:
          name: yado-g2-native-contextual-ast-transformer-gene-v7-${{ github.run_id }}
          path: |
            candidates/kernel-self-generated/g2-native-contextual-ast-transformer-gene-genesis-v7.json
            candidates/kernel-self-generated/g2-native-contextual-ast-transformer-gene-v7.json
          if-no-files-found: warn
