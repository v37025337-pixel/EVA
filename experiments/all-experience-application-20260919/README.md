# Apply available accumulated experience

This finite campaign resumes the 1693-event checkpoint from run 35456725134.
It preserves identity, native programs, the admitted numerical runtime module,
web memory and the complete old event prefix. No historical verdict is promoted
to a fresh result.

The existing lossless archive builder freezes all reachable objects from every
fetched ref, including deleted file history. Its verifier rechecks payloads and
Git identities. The inherited birth archive remains intact. Coverage reports
distinguish archived claims, native observations and executable components;
archiving all bytes is not a claim that every document was understood or used.

The existing `GenerationKernel` discovers byte-matched historical components,
measures all supported options, rejects regressions, freezes its selected profile,
and admits it only after fresh checks and retained-memory replay. Four bounded
families are supported: Boolean logic, constrained planning, routing and exact
polynomial code synthesis. The candidate's baseline is the component executor's
parent profile, not a measurement of all YADO capabilities.

After reopening the persistent component executor, the campaign executes five
fresh batches (20 tasks) through its active profile. This profile lives in
`component-evolution/state.sqlite`, with its pinned `experience.sqlite`; it does
not silently replace the primary kernel's dispatch table. Preserve both files
when continuing. This campaign copies and reopens them on subsequent invocations;
an existing activation is reused, never reported as a new improvement. New Git
history is still archived separately on continuation.

The native kernel then selects retries from its full recorded experience and
runs ten autonomous cycles. The host supplies no new native task answers. Empty
retry sets and unchanged program counts are reported honestly. All native
results are rechecked and prior runtime strategies must remain active.

The assistant authors orchestration; existing YADO mechanisms select profiles,
generate programs and choose native goals. This is measured reuse and bounded
component development. Checkpoint integrity, fresh performance and historical
preservation are separate requirements. CI must pass before merge.
