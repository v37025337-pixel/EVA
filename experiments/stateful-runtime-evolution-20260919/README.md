# Measured runtime evolution from retained experience

This finite experiment originally targeted checkpoint 35453219380. The unified
workflow now defaults to checkpoint 35454873271, retaining its 1334-event journal,
operational identity, source overlay and 14 web-research episodes. An explicit
`--source-updates` map admits the reviewed canonical/router transition when
resuming across that implementation change; no undeclared drift is accepted.
It calls the existing runtime-evolution selector before introducing any task.
If no unresolved supported failure exists, that negative proposal is preserved.

The assistant-authored observation bridge projects previously verified numeric
examples onto y=0 and asks native cognition to emit executable programs. An exact
interpolation oracle derives validation values. These are disclosed probes from
old experience, not new blind-transfer evidence. Successful probes can seed up
to three challenges using the existing polynomial-composition grammar. Both the
bridge and the inherited grammar have human/assistant origins. No new solving
algorithm is written by this experiment.

On a real failed probe, the unchanged kernel selector chooses the deficit and
emits a reusable module from its inherited exact polynomial fitter. Its bytes
are frozen before new random transfer cases. The existing gate runs the complete
repository regression with that module, negative cases, retained cognitive
results and the full kernel audit. Admission requires every gate to pass and a
measured gain over the previous native route and admitted modules. The failed
task is then retried through normal cognition, and retained results are checked
again. Failed gates remain failed; the harness does not retry or relabel them.

The checkpoint and evidence are saved even when a cycle withholds admission.
The continuation receipt attests state integrity separately from the evolution
verdict. Reopening must preserve the old event prefix exactly. No runtime source
or canonical file is patched; an admitted strategy lives in the durable journal.

Run the existing External Dev Self Development workflow with `evolve_runtime`
enabled and the exact predecessor run/artifact, or push this experiment's named
branch. The result artifact retains the existing `yado-repository-learning-`
checkpoint prefix for compatibility. This does not create a permanent service,
invent an unrestricted algorithm, or establish general intelligence or consciousness.
