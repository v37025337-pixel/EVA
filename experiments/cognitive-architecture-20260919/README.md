# Unified cognitive development continuation

This user-authorized maintenance binding consolidates the remaining learning
feed history and pins the existing web research and cognitive validation modules
in the single G2 core. The assistant wrote the integration and campaign setup.
YADO's existing selectors choose goals from durable experience.

The canonical runtime source manifest grows from 109 to 127 files. No formal
generation change or new general capability is claimed. Cognitive evaluators are
bound as shadow validation modules, not promoted capabilities.

`feed-verification.json` verifies both incoming feed commits against the actual
canonical identities of their successful originating workflow runs. The generated
recall files remain shadow candidates. Their old provenance is preserved.

`verify_cognition.py` re-executes five existing fixed-seed benchmarks and preserves
their outputs in `cognitive-results/`, restoring the repository's previous shadow
candidates afterward. All five local gates pass. Fresh split results are logic
40/40, thinking 32/32, transfer 56/56, memory 32/32, integration 50/50; removing
each integration component drops accuracy by 0.2. The selected strategies were
already successful: this is revalidation, not a newly measured gain. Changes in
emitted source formatting or score metadata are not intelligence growth.

The first live workflow restored checkpoint run **35453219380**, tick **1142**,
and completed successfully as run **35454873271** at tick **1334**, with all 20
endogenous cycles verified, 11 to 14 research episodes, preserved identity and
history, and no new verified native programs. Its summary is preserved in
`first-continuation-summary.json`.

The next workflow restores that **1334** checkpoint, verifies its recorded file
hashes, and uses `source-update.json` to admit exactly five changes (three
canonical files, the router loader and its tests). The operational identity and complete old event
prefix must survive. It then performs real public-web research, the pinned local
Hivemind MCP task, up to three native-program development rounds, and 20
kernel-selected endogenous cycles. An empty eligible-deficit set is recorded as
such; the campaign does not fabricate goals or claim new verified programs.

Before launch, an older local fixture (run 35452596993, tick 954) passed the exact
source transition, rejected an empty transition map, preserved its identity and
history, and completed the native development call with no eligible experience.
This local fixture test is distinct from the requested latest-state live run.
Canonical invariant, active implementation and full kernel audit V2 checks also
pass locally. CI and the latest-state live campaign remain required before merging.

The expanded source manifest exposed an existing dynamic router execution site
in the security audit. The repair removes `exec` entirely: the loader parses four
bounded literal slots, validates their types, sizes and admitted capabilities,
requires exact equality with the trusted renderer, and interprets only the
validated routing data. Four new rejection tests cover injected side effects,
function replacement, nonliteral expressions and invalid policy budgets. All 11
router and campaign tests pass locally. Existing audit rules are unchanged.
The second maintenance event appends to the first campaign's ledger event;
neither canonical event is rewritten.

The workflow preserves a complete new checkpoint as an artifact. Its run summary
reports actual program additions and event counts. Completion does not leave a
background process running. Existing recurring learning is a separate workflow.
