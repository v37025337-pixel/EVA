{
  "schema": "yado.architecture.external_project_evolution.v2",
  "component_id": "RUNTIME-G2-THREE-STATE-DERIVED-EVOLUTION-V2",
  "generation": "G2",
  "purpose": "Prove three consecutive state-derived training generations over admitted external project observations.",
  "generations": [
    {"id": "G1", "input": "source observations", "output": "initial capability profiles"},
    {"id": "G2", "input": "G1 plus successful seed tasks", "output": "experience-conditioned profiles"},
    {"id": "G3", "input": "G2 plus distinct adaptation tasks", "output": "holdout-verified profiles"}
  ],
  "required_proof": [
    "each generation has a distinct training digest",
    "G2 replays G1 seed tasks",
    "G3 passes three unseen holdout tasks",
    "unknown semantics WITHHOLD",
    "canonical mutation remains externally gated"
  ],
  "boundary": {
    "third_party_code_executed": false,
    "external_writes": false,
    "automatic_main_mutation": false,
    "g4_genesis_performed": false
  }
}
