{
  "schema": "yado.g2.continuity_file_integrity.request.v2",
  "objective": "PRESERVE_EXACT_RUNTIME_TYPES_AND_FAIL_CLOSED_FILE_INTEGRITY_ACROSS_PROCESS_RESTART_BEFORE_NOVEL_GENE_GENESIS_V4",
  "task": "Evaluate an assistant-authored infrastructure successor of canonical execution fabric V4. Keep the in-memory checkpoint schema at yado.g2.cognitive_continuity_checkpoint.v1, but write new checkpoint files as yado.g2.cognitive_continuity_file.v2 typed envelopes. Preserve integer dictionary keys, tuples and exact Fraction values; reject unsupported objects and non-finite numbers; verify file, checkpoint, temporal, recurrent-memory and cross-layer integrity before mutating the supplied base runtime; keep valid legacy plain-JSON v1 compatibility; use unique temporary files, fsync, atomic replace, cleanup on failed replace and a 16 MiB file limit. Run focused real Logic, Intelligence and Thinking regressions including a separate Python process restart. Do not claim kernel-generated source or intelligence gain.",
  "source_basis": {
    "user_file": "g2-continuity-file-integrity.md",
    "key_contracts": [
      "new_file_schema_yado.g2.cognitive_continuity_file.v2",
      "in_memory_checkpoint_schema_remains_v1",
      "preserve_integer_keys_tuples_fraction",
      "legacy_valid_v1_accepted",
      "pre_restore_full_integrity_validation",
      "unique_temp_fsync_atomic_replace_directory_fsync_attempt",
      "failed_replace_preserves_previous_checkpoint",
      "single_writer_per_checkpoint_path",
      "max_file_16_MiB",
      "unsupported_and_nonfinite_fail_closed",
      "no_executable_object_deserialization",
      "same_programs_api_policy_and_canonical_generation_supplied_by_caller",
      "assistant_authored_repair_not_self_generated_source"
    ]
  },
  "constraints": {
    "canonical_mutation": false,
    "automatic_promotion": false,
    "shadow_only": true,
    "kernel_generated_source_claim": false,
    "general_intelligence_gain_claim": false,
    "external_models": false,
    "focused_regressions_required": true,
    "canonical_guard_before_and_after": true
  },
  "success_conditions": [
    "real Logic symmetric count-map preserves integer keys and same answer after restart",
    "real Logic exact polynomial preserves tuple basis and Fraction coefficients",
    "real Intelligence tuple output survives restart",
    "real Thinking memory feedback survives restart",
    "separate Python process restores exact types and behavior",
    "valid legacy plain-JSON v1 remains loadable",
    "bounded recurrent memory remains bounded after file roundtrip",
    "stall continuity survives restart",
    "corrupt file fails before base runtime mutation",
    "cross-layer forgery fails before base runtime mutation",
    "failed replace leaves previous checkpoint intact and removes temp file",
    "unsupported objects and non-finite numbers fail closed",
    "raw file envelope is not accepted as in-memory continuity_state",
    "16 MiB file bound enforced",
    "canonical V4 remains unchanged during shadow test"
  ],
  "host_role": "ASSISTANT_AUTHORED_INFRASTRUCTURE_REPAIR_AND_STRICT_VERIFICATION_ONLY",
  "stop_condition": "CONTINUITY_FILE_INTEGRITY_PASS_SHADOW_OR_EXPLICIT_FAILURE",
  "trigger": 1,
  "trigger_reason": "APPLY_USER_PROVIDED_G2_CONTINUITY_FILE_INTEGRITY_REQUIREMENTS_BEFORE_EXPERIENCE_CONDITIONED_NOVEL_GENE_GENESIS_V4"
}
